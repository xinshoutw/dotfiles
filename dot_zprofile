# Homebrew (ARM on Apple Silicon)
if [ -x /opt/homebrew/bin/brew ]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi

# SSH Agent Socket
export SSH_AUTH_SOCK=~/.bitwarden-ssh-agent.sock

# X11 Display
export DISPLAY=:0

